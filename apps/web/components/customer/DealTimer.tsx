/**
 * "Top picks" label for the home products row. There is no real promotional
 * engine yet, so we avoid any countdown or "deal ends in" claim — products are
 * simply in-stock items from nearby shops.
 */
export function DealTimer() {
  return null
}
