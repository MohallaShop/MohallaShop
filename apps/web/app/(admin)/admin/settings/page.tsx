import { PagePlaceholder } from '@/components/layout/PagePlaceholder'
export default function AdminSettings() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Platform configuration and feature flags ship in a later phase."
      phase="Later"
    />
  )
}
