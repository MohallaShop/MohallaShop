import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export default function RiderDeliveries() {
  return (
    <PagePlaceholder
      title="Deliveries"
      description="Active and past deliveries with pickup/dropoff actions arrive with the rider lifecycle (Phase 1b)."
      phase="Phase 1b"
    />
  )
}
